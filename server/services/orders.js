import prisma from "../prisma/client.js";

const createOrder = async ({ name, mobile, address, city, pincode, country, state, billingName, billingCity, billingPincode, billingCountry, billingState, notes, orderProducts }) => {
  
  return user;
};

export default {
  createOrder
};
